git init .                  // initialize git
git status                  // to see status of files
git add                     // filename to add file in order to get track
git log                     // shows commits history
git commit                  // to finalize the changes
git config --global user.email "you@example.com"    //to tell emailId of gitAcc
git config --global user.name "Karansehgal15"       // to tell userName of gitAcc
git checkout -b newbranch_name                      //to create newbranch and it switches to that
git branch                  // shows current branch by * and all other branch name
git checkout BranchName // to switch to any other existingBrach 
git merge BranchName // will merge this Branch with Name to current branch
git config --global alias.short_name "complete_command_is_written_here" // to create alias for command 
git commit --amend              // to edit latest commit in local repo use only when it is not pushed to github server
git push -f origin Branch_name__   //to forcefully push the changes to the branch used when normal push fails